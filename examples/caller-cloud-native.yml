name: Organization OWASP Cloud-Native App Security
on: [push, pull_request]
jobs:
  scan:
    uses: ORG/security-workflows/.github/workflows/reusable-security.yml@main
    with:
      app_type: cloud-native
      target_url: https://service-test.example.internal
      dast_mode: full
    secrets: inherit
