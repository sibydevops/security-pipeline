name: Organization OWASP Security
on: [push, pull_request]
jobs:
  scan:
    uses: ORG/security-workflows/.github/workflows/reusable-security.yml@main
    with:
      app_type: web
      target_url: https://pr-app.example.internal
      dast_mode: baseline
    secrets: inherit
