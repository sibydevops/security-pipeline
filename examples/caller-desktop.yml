name: Organization OWASP Desktop Security
on: [push, pull_request]
jobs:
  scan:
    uses: ORG/security-workflows/.github/workflows/reusable-security.yml@main
    with:
      app_type: desktop
      dast_mode: none
    secrets: inherit
